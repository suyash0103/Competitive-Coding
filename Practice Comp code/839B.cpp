#include<bits/stdc++.h>

using namespace std;

int arr[10000];

int main()
{
	int n, k, sum = 0;
	scanf("%d%d", &n, &k);
	for(int i = 0; i < k; i++)
	{
		scanf("%d", &a[i]);
		sum = sum + a[i];
	}
	if(sum > 8 * n)
	{
		printf("NO");
		exit(0);
	}
	if(n == k)
	{
		
	}
	else
	{
		//int filled[10000][8] = {0};
		for(int i = 0; i < k; i++)
		{
			if(a[i] >= 4)
			{
				
			}
		}
	}
}
